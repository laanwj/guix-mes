;; guile: 00
;; mes: segfault
;; (display
;;   (let ((count (let ((counter 0))
;;                  (lambda ()
;;                    counter))))
;;     (count)))

(display
 ((lambda (count)
    (count))
  ((lambda (counter)
     (lambda ()
       counter))
   0)))
(newline)
